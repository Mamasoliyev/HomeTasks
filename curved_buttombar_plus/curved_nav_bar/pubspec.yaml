name: curved_nav_bar
description: "A new Flutter project."
version: 0.1.0
publish_to: 'none' # remove this if you publish to pub.dev

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  curved_navbar_plus: ^0.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
